<div class="entry">
    <img src="{{product.image_url}}" alt="Picture of {{product.title}}"/>
    <h3>{{product.title}}</h3>
    <p>{{product.description}}</p>
    <div class="price_line"><span class="price">{{product.price}}</span></div>
</div>
