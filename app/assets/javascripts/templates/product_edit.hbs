<form {{action save on="submit"}} class="form-horizontal">
  <legend>Edit Product</legend>

  {{partial "product_edit_fields"}}

  <div class="form-actions">
    {{submitButton "Update"}}
    <button class="btn" {{action cancel}}>Cancel</button>
  </div>
</form>
